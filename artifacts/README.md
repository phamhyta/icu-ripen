# artifacts/

Derived per-stay monitoring cohorts (`cohort.npz`, `cohort_challenge2012.npz`, `cohort_eicu.npz`)
are built locally by `code/extract.py` and `code/extract_external.py`. They are **not
redistributed** here because they derive from credentialed PhysioNet data — see the data
governance section of the top-level README.
